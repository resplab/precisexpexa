% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/precisex-core.R
\name{PREDICT}
\alias{PREDICT}
\title{Run the full PRECISE-X prediction}
\usage{
PREDICT(patient, include_b = FALSE)
}
\arguments{
\item{patient}{Named numeric vector of predictors. Must include the
mandatory `female`, `age`, `mrc`, and either `fev1` or `fev1pp`.}

\item{include_b}{Logical. If `FALSE` (default), the imputation flags
(`b_*`) are zeroed before the linear predictor is computed, matching the
upstream default.}
}
\value{
A list with components `patient`..`patient5` (intermediate states),
  `lin` (linear predictor), `preds` (named 5-year exacerbation
  probabilities) and `plot` (a ggplot object).
}
\description{
Orchestrates processing, imputation, boundary application, interaction-term
construction, and the final survival calculation.
}
\keyword{internal}
